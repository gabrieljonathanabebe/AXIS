export type ChartTheme = {
  accent: string
  axis: string
  text: string
  textMuted: string
  tooltip: {
    background: string
    borderColor: string
    borderWidth: number
    fontSize: number
    blur: string
    radius: string
    shadow: string
  }
}
